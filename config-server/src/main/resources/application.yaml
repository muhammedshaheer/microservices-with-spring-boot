spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/muhammedshaheer/microservices-with-spring-boot-config-server.git

server:
  port: 8888